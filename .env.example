# Twilio credentials (from https://console.twilio.com)
TWILIO_AUTH_TOKEN=your_twilio_auth_token

# Google Service Account JSON as a single-line string.
# To create: Google Cloud Console → IAM & Admin → Service Accounts → Create → Keys → JSON.
# Copy the entire JSON and minify to one line, or use: python -c "import json; print(json.dumps(json.load(open('key.json'))))"
GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"...@....iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}

# Google Sheet ID from the sheet URL: https://docs.google.com/spreadsheets/d/SHEET_KEY/edit
SHEET_KEY=your_google_sheet_id

# Port (Railway sets this automatically)
PORT=8000
